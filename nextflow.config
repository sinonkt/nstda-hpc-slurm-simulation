process {
    withName:simulate {
        container = 'shub://sinonkt/docker-slurm-simulator:latest'
    }
}

profiles {
  standard {
      process.executor = 'local'
  }
}

singularity {
    enabled = true
}